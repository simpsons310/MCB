def compute(a,b,opr):
	 sortA(a[],c):
	int temp
	length = sizeof(a)/sizeof(int)
	for i in range(0,length-2)
		for j range(i+1,length-1)
			if (c = "asc")
				if(a[i] > a[j])
					a[i] = temp;
					a[i] = a[j];
					a[j] = temp;
			else if (c = "desc")
				if(a[i] < a[j])
					a[i] = temp;
					a[i] = a[j];
					a[j] = temp;
